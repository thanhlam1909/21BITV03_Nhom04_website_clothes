@model IEnumerable<_21BITV03_Nhom04_website_clothes.Data.ProductColor>

@{
    ViewData["Title"] = "Product Colors";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="container-fluid px-4">
    <h1 class="mt-4">Product Colors</h1>
    <ol class="breadcrumb mb-4">
        <li class="breadcrumb-item"><a asp-controller="Admin" asp-action="Index">Home</a></li>
        <li class="breadcrumb-item active">Product Colors</li>
    </ol>

    <p>
        <a asp-action="Create" class="btn btn-primary">Create Product Color</a>
    </p>

    <div class="card mb-4">
        <div class="card-header">
            <i class="fas fa-palette me-1"></i>
            Product Color List
        </div>
        <div class="card-body">
            <table id="datatablesSimple" class="table table-bordered">
                <thead>
                    <tr>
                        <th>@Html.DisplayNameFor(model => model.First().ColorName)</th>
                        <th>@Html.DisplayNameFor(model => model.First().Description)</th>
                        <th style="text-align: center;">Actions</th>
                    </tr>
                </thead>
                <tfoot>
                    <tr>
                        <th>@Html.DisplayNameFor(model => model.First().ColorName)</th>
                        <th>@Html.DisplayNameFor(model => model.First().Description)</th>
                        <th style="text-align: center;">Actions</th>
                    </tr>
                </tfoot>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@Html.DisplayFor(modelItem => item.ColorName)</td>
                            <td>@Html.DisplayFor(modelItem => item.Description)</td>
                            <td style="text-align: center;">
                                <a asp-action="Edit" asp-route-id="@item.ColorId" class="btn btn-sm btn-warning" title="Edit">
                                    <i class="fas fa-edit"></i> Edit
                                </a>
                                <a asp-action="Delete" asp-route-id="@item.ColorId" class="btn btn-sm btn-danger" title="Delete">
                                    <i class="fas fa-trash"></i> Delete
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
